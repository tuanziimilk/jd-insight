# 需要人工复核的东西

> 这份文件回答一个问题：**这套系统里哪些数字和词表是"我（AI）推测的"，
> 错了会导致什么，以及你怎么改。**
>
> 为什么需要它：这个项目里大量结论建立在词典、权重和阈值之上，
> 而这些东西绝大多数**只能改代码、没有界面**。不写下来，它们就是黑箱 ——
> 而黑箱的代价不是报错，是**给你一个看起来很专业的错结论**。
>
> 建立于 2026-09-10。**每次改动这些产物时，同步更新对应行的「上次复核」。**

---

## 已经出过的事故（留档，防同类复发）

| 日期 | 什么 | 为什么没被发现 |
|---|---|---|
| 2026-09-10 发现 | `analyzer/config.py` 与 `config.example.py` **逐字节相同**，`MY_PROFILE` 一直是模板占位（每条带「示例」），而报告把它当真实能力印出来、据此排「你最该补 RAG / 对话式产品」 | 原来的 `USING_EXAMPLE` 只判断「`config.py` 存不存在」。文件存在、内容是示例 → 警告一次都没打过 |
| 2026-09-10 发现 | 两份报告样本量 4 条 / 3 条，而报告头部自己写着「样本少于 10 条时结论不稳」，下面照样输出带 🔥🔥 的优先级排行 | **那句话只是提示，代码没有执行它。** 一条写在文档里、代码不执行的规则等于没有规则 |
| 2026-09-11 发现 | 题目 id 是位置型（`m1-q7` = 第 7 条）。往题库中间插一道题，其后所有作答记录静默挂到别的题上 | 没有外键、没有约束能发现孤儿行。而"扩展题库"正是日常要做的事 |
| 2026-09-11 发现 | **导入脚本一直在静默清空题库**：`面试题库.md` 是 CRLF，而 JS 的 `.` 不匹配回车符，题库正则的 `(.+)$` 一条都匹配不上 | 模块正则没锚 `$`，照常工作 → 症状是「模块全在、题目 0 条、不报错」，然后覆盖写成 `[]`。而那 39 条是文件还是 LF 时生成的，之后没人重跑过 |

后三条都已修（内容派生 id + 行尾统一 + 两道新闸门），见下。

前两条已修（`analyze_jd.py` 加了占位闸门 + 样本量闸门，行为测试在 `analyzer/test_gates.py`）。

---

## 一、现在就该你动手的

| 产物 | 位置 | 怎么改 | 上次复核 |
|---|---|---|---|
| **`MY_PROFILE` 真实自评** | `analyzer/config.py`（已被 `.gitignore` 排除，不会进仓库） | 按 `GROUPS` 里的组名逐个填：🟢 有真实项目支撑 / 🟡 概念懂但缺实操 / 🔴 空白。**填之前分析器会拒绝输出缺口排行** | ❌ 从未填过 |
| **`career_profile.capabilities` 能力自评** | Supabase 表 | 🟢 **已有入口**：career-web 的 **07 校准台**。组名从技能词典派生（不给手填 —— 词典要求两边逐字一致，手打一个错字就静默少算一整组）。**04 匹配整页依赖它** | ❌ 从未填过 |

---

## 二、影响结论、只能改代码（🔴 = 无界面）

### 技能词典 `career-web/src/data/skills.json`（权威源）

改完必须跑 `cd career-web && npm run check:shared -- --fix` 重新生成扩展侧副本。

> 🟢 **现在能看见了**：career-web 的 **07 校准台**有一节「技能词典体检」——
> 26 项的权重、patterns、校准记录全部摊开，并且**拿你真实采集的 JD 现场跑一遍**，
> 显示每一项命中了哪些原句、哪些 pattern 真被验证过（绿底）、哪些还只是猜的（白底）。
> 词典本身**仍然只能改代码**（它是编译进产物的静态文件），要可写得先搬到云端表（P2）。
> 但"看不见"和"改不了"是两个问题，而看不见才是黑箱感的主要来源。

| 字段 | 现状 | 错了会怎样 | 上次复核 |
|---|---|---|---|
| 26 项的 `weight`（1/2/3） | 🔴 **人工判断**，依据是「这项对我这份简历有多核心」 | `priority = jdCount × weight`，直接决定「先补哪个」的排序。**它是个人判断，但输出时表现为客观排行** | 2026-09-10（v2.2） |
| `domain.coreSkills` 9 项 | 🔴 人工挑选 | 决定一条 JD 是否被**整条丢弃**（`gap.js` 的 `inDomain()`） | 2026-09-10 |
| 约 280 个 `patterns` | 🔴 **人写和 AI 猜混在一起，没有任何标记区分** | 漏词 = 整句漏判。只有 v2.2 那三条记录了「来自真实 JD 反查」 | 2026-09-10（v2.2） |
| `calibration` 数组 | 词典的修改史与理由 —— **全项目唯一说明"为什么是这个数"的地方** | 不维护它，三个月后没人敢改词典 | 2026-09-10 |
| `gates`（years / degree） | 刻意**不进**缺口列表（门槛不是能补的能力） | — | 2026-09-10 |

**已知不一致**：`finance`（金融行业经验）按 v2.2 定的原则（行业背景是门槛不是能力）本该删掉，`calibration` 里自认是「历史遗留」。

### 红线与意图路由 `jd-insight/extension/lib/intents.js`

| 词表 | 规模 | 错了会怎样 | 上次复核 |
|---|---|---|---|
| **`FAB_CUE`** | 13 词 | 🚨 **安全**。漏词 = 编经历请求漏进生成路径 | 2026-09-10 |
| **`WRITE_REQ`** | 11 模式 | 🚨 同上。`FAB_CUE` **AND** `WRITE_REQ` 才算红线（刻意的：裸词「没做过」出现在 GAP 主功能的问法里） | 2026-09-10 |
| **`FACT_FIELD`** | 12 字段 × 5 动词 | 🚨 单信号即拦。改职位名/学历/时间没有正当解读 | 2026-09-10 |
| `GAP_PAT` / `DIAGNOSE_PAT` | 13 / 8 | 两者边界靠**书写顺序**决定（GAP 先判）—— 注释自认「靠规则书写顺序决定谁赢等于靠巧合」 | 2026-09-10 |
| `KNOWN` 关键词 | 29 个 | 🔴 **最脆弱的一处**。同时驱动 STATS 宾语抽取和 `rule:kw` 捷径。新领域词不加进去，整类问题的路由就变了 | 2026-09-10 |
| `systemPrompt` 的 5 条硬规则 + 拼装顺序 | — | ⚠️ **顺序 = 成本**。各家上下文缓存都是前缀匹配，把意图指令放到简历前面会静默把几千 token 踢出缓存。**动错顺序不报错，只变贵** | 2026-09-10 |

### 阈值与魔法数字

> 🟢 **现在有登记表了**：`career-web/src/data/thresholds.json` 登记了 18 项
> （值 / 依据 / 被测量过没有 / 改错了会怎样），**07 校准台**有一栏显示它。
> `npm run check:thresholds` 会去源文件里逐字核对，改一边不改另一边就让 build 失败 ——
> 所以那张表不是文档，是契约。
>
> ⚠️ 常量**仍在各自源文件里**，没有搬到一处：那会成为第四个会漂的副本
> （前三个里已经有一个没人看住）。登记 + 校验的风险比"共享常量"小。
>
> ⚠️ 那个脚本保证不了「源码里所有阈值都登记了」—— 新加影响结论的常量时要手动补。
>
> 登记结果本身就是个答案：**有正当依据 3 项、数字是拍的 9 项、完全没依据 6 项。**

下面这张表保留，因为它按「在哪个文件」组织，便于改代码时对照：


| 常量 | 值 | 有没有依据 | 位置 |
|---|---|---|---|
| `K1` / `B` | 1.2 / 0.75 | 🟢 **BM25 论文标准值 —— 全项目唯一有正当依据的两个** | `retrieve.js` |
| `MIN_JDS` | 5 | 🟡 方向有理由（"3 条里 2 条提到某词是巧合"），**5 这个数是拍的** | `gap.js` |
| `MIN_BODY` / `MIN_PAGETEXT` | 120 / 200 | 🟡 / 🔴 前者有实测背景（真实详情页 400~1200 字），后者**无任何说明** | `gap.js` + `match.ts` |
| `evidenceScore` 的四个数 | 0.12 / +3 / +1 / −3 | 🔴 全无依据。而它决定"引哪句原文当依据"，**依据是整条结论的全部可信度来源** | `gap.js` + `match.ts`（**两份，无校验**） |
| `WEIGHT` 字段加权 | title 3 / company 2.5 / tagline 1.5 / body 1 | 🔴 拍的，直接决定检索排序 | `retrieve.js` |
| `EXPANSION_WEIGHT` / `MAX_EXPANSION_TERMS` | 0.45 / 24 | 🟡 / 🔴 前者有方向性理由（扩展词是推测的，不能和用户真打的词等权），**无调参记录** | `retrieve.js` |
| `MIN_PLAUSIBLE` / `MAX_BAD` / `MIN_SAMPLES` | 0.85 / 1 / 3 | 🟡 前两个有实测反证记录，`MIN_SAMPLES` 无 | `glyphmap.js` —— **薪资字形还原的唯一防线，错了会输出看起来正常、实际错位的薪资** |
| `LEVEL_SCORE` 的 🟡 | 0.5 | 🔴 拍的，**直接换算成你看到的「匹配覆盖率 XX%」** | `match.ts` |
| `silentDays` | 14 | 🔴 决定"该跟进了" | `pipeline.js` + `funnel.ts`（**各写一份，无校验**） |
| `HOVER_DWELL` | 400ms | 🟡 有理由（鼠标横穿的卡片不该被存错） | `content.js` |
| `MIN_SAMPLE`（分析器） | 10 | 🟢 和报告头部那句提示一致，2026-09-10 起代码真的执行它 | `analyze_jd.py` |

### 会过期的外部事实

| 产物 | 现状 | 上次核对 |
|---|---|---|
| 18 条模型价格 + `USD_CNY = 7.2` | `providers.js`。**所有 `asOf` 都是 2026-09-10 同一天抄的**。7 条缓存命中价标为「未知」（火山 3 条 + 硅基流动 4 条）—— 查不到就不编 | 2026-09-10 |
| ⚠️ `deepseek-v4-pro` 计价 | 官方文档提到 **2026-09-14 起**切到 V4.1 Flash 计价 | **到期，需核对** |
| BOSS DOM 选择器 | `content.js` 的 `RECO_SEL` / `DETAIL_SEL` / `CARD_SEL`。BOSS 改一次类名就失效，表现是「存 JD 报没抓到内容」 | 2026-09-10 |

**价格没有编辑界面**：原来设置页有一张可编辑的价格表，被整段删了 —— 因为它只在 `s.pricing` 为空时才铺内置价，老配置里有一行遗留数据导致 **18 个内置价一个都没显示**。「一个把正确数据挡住的编辑界面，比没有这个界面差。」现在设置页只**显示**参考价 + `asOf` + 官方定价页链接。

### 脱敏规则

| 产物 | 规模 | 缺口 |
|---|---|---|
**2026-09-11（P1.4）之后变成四层，前三层在 `import-content.mjs`，第四层是人：**

| 层 | 产物 | 规模 | 它拦不住什么 |
|---|---|---|---|
| 1 | `REDACT` 替换词 | 6 条（公司全名、OMS、AI Center 三种写法、**在投目标公司名 + 其产品名**） | **引文指纹**。实测案例：公司名被换成「某目标公司」，同一行后半句仍逐字引着公司愿景原话 —— 那两句话随便一搜就知道是谁 |
| 2 | `EXCLUDE_TITLE_PATTERNS` 整卡排除 | 5 个（`/现状自检/ /短板/ /自我评估/ /面试怎么说/ /写成作品/`） | 标题正当、正文夹自述的情形。也**完全不作用于题库解析**（它只在模块解析里被调用） |
| 3 | `DROP_LINE_PATTERNS` 行级丢弃 | 8 条 | 没有明确行首标记的自述 |
| 4 | `scanLeaks()` 对**产物**扫词表（`scripts/leaks.mjs`）+ `content-review.json` 人工签章 | 词表 18 条 / 签章逐条记哈希 | 词表只认它认识的词，签章只证明有人看过 —— **都不证明产物是干净的** |

⚠️ **前三层的共同弱点是它们只拦我想到的情形。** 两个已查实的「靠运气挡住」：

1. 题库里「→ 归 M9 图 5 Reliability（**你的短板**）」「（**头号短板**）」「以下是**你目前答不出**的题」—— 现在没进产物的唯一原因是它们在 `## 📦 外部面经补充题` 那一节，而解析在 `## 通用必考题` 处就 break 了。**保护它们的是一个为了别的目的写的 break。**
2. M10 第 1 节标题「逐条是什么」完全正当，正文却夹着「**和我的连接**：我的 Obsidian 双链库…」—— 没露出来只是因为 `excerpt` 截断在 600 字。

第 4 层就是为这两件事加的：它**不看规则、不看来源，只看产物里最后有什么字**。

⚠️ 词表刻意只收「出现即泄漏」的词。差点写进 `/面试/` —— 十个模块里有 22 个标题带"面试"（「面试高频问答」「术语中英对照（面试用）」），那会删掉一大片正当知识，然后我就会学会忽略这个闸门。`eval-leaks.mjs` 里有 11 条「不该抓」的断言专门钉这件事。

---

## 三、评测集：断言基于什么

| 脚本 | 规模 | fixture 来源 |
|---|---|---|
| `eval-retrieve.mjs` | 12 语料 / 10 标注用例 | ⚠️ **构造的** —— 岗位名和公司取自真实截图，**正文是按真实 JD 的写法编的**。价值在"改动前后可比"，不在"绝对准确" |
| `eval-gap.mjs` | 7 条完整长度 fixture | 🟡 **重建的** —— 公司/岗位/薪资/能力要求来自真实采集记录，被压缩掉的段落按真实写法补回原长。⚠️ **唯一带真实公司名的评测数据**（携程/拼多多/滴滴等），公开后可反推采集记录 |
| `eval-intents.mjs` | 21 用例 | 纯 query 字符串。含 5 条 **MODEL 类 = 如实登记的已知覆盖洞**，不假装覆盖 |
| `eval-pricing.mjs` | 18 模型全扫 | 目录本身。查结构性事实（id 跨厂商唯一等），**查不了"这个数字是否还是今天的价"** |
| `analyzer/test_gates.py` | 10 条断言 | 两个方向都测：占位不能漏拦、真自评不能误伤 |
| `check-syntax.mjs` | 全部扩展 js | 真 ESM 解析 —— **`node --check` 对 ESM 是假通过**，别拿它当闸门 |

`eval-gap.mjs` 里那份 `RESUME` 常量是**为测试写的简化简历**，不是你的真简历，改真简历时它不会跟着变。

---

## 四、机器闸门覆盖了什么、没覆盖什么

**有闸门**（`career-web/scripts/check-shared.mjs`，挂在 `npm run build` 上）：

| 共享物 | 权威方 | 校验方式 |
|---|---|---|
| `salary.js` | **扩展** | 字节级相同 |
| `skills.json → extension/lib/skills.js` | **工作台** | 内容一致（生成 ESM 模块，不用 JSON 模块导入 —— 那需要 Chrome 123+，不支持时整个情报台在模块加载阶段白屏） |

**2026-09-11 新增两道闸门**（都挂在 `npm run build` 上，都验证过会让 build 失败）：

| 脚本 | 查什么 | 防的是 |
|---|---|---|
| `check-line-endings.mjs` | Node 侧「读文件 + 按行切分」的地方有没有做行尾统一 | CRLF 让 `(.+)$` 整段匹配不上，而且不报错 |
| `import-content.mjs` 里的 `guardShrink` | 产出条数比已有产物少一半以上就拒绝写入 | 「解析悄悄失效 → 覆盖写 → 数据没了」这个**模式本身**，不只是 CRLF |
| `check-diagrams.mjs` | `modules.json` 里 M9 的六张 mermaid 图源**和知识库原文逐字相同** | 图源被截断 —— 半张 flowchart 是语法错误，渲染器只会给一个报错框 |
| `import-content.mjs` 里的 `guardDiagrams` | M9 解析出 0 张图就拒绝写入 | 围栏写法变了/正则被改坏 → 脑图页变空页面，且不报错 |
| `import-content.mjs` 里的 `guardLeaks` | 产物里扫到自评/公司名/在投公司相关的词就**拒绝写入** | 隐私内容进产物 —— 一旦提交进公开仓库，git 历史里就永远有了 |
| `eval-leaks.mjs` | 词表对 13 条**真实泄漏原文**会红、对 11 条**正当知识内容**不会红 | 「一道从不报错的闸门等于没有闸门」和「一道会误报的闸门比没有更糟」，两头都钉住 |
| `review-content.mjs` | 产物和上次人工签章那一版逐条比，变了就让 build 失败并**列出是哪几条** | 「必须人工 review」这条硬性红线此前的全部执行机制是在终端打三行提示，**没有任何东西记录它有没有被执行过** |

**2026-09-11 第二批（P1.5~P1.8）**：

| 脚本 | 查什么 | 防的是 |
|---|---|---|
| `check-shared.mjs` 第三节 | 漏斗常量两端一致 + `silentDays` 判定**按行为**比 | 两端各写一份、改一边另一边不报错。第一次跑就抓到 `TERMINAL` 的分叉 |
| `eval-learning-progress.mjs` | 学习进度的分母永远来自内容，不来自进度记录行数 | 首页显示 2/9、学习页显示 2/10，而且**越标记越退步**（分母跟着进度行数涨） |
| `eval-row-version.mjs` | 多端并发写的冲突检测（用假数据库构造"另一端改过了"） | 无条件 upsert = 最后写的赢。手机标的进度被电脑上的旧标签页覆盖，且不报错 |
| `eval-cite.mjs` | 引用校验：编号越界 / 整段没出处 / 错位嫌疑 | 「引用错位是最隐蔽的幻觉」—— 带出处的句子看起来更可信，而没人会真去点开核 |
| `eval-errors.mjs` | 错误分类（假 `fetch`）+ 文案里有没有「怎么办」 | golden F 组四条一直没跑过，而它考的其实全是本地逻辑 |
| `eval-all.mjs` | 一条命令跑完扩展侧七个脚本 | 扩展零构建、没有 `npm run build` 那样的强制入口，全靠"记得跑"

⚠️ `check-diagrams.mjs` 里有一条断言**只在这台机器上真正有效**：
逐字比对要读 `career-knowledgebase/03-学习笔记/M9-Agent系统脑图.md`，
而知识库不在版本控制里。源文件缺失时那一节**跳过并明说自己跳过了** ——
没有源头的机器上，剩下的只有覆盖不全的结构性断言。

⚠️ 这道闸门的结构性断言是补出来的，起因是我自己验证时的一次失败：
最初只有「首行是图类型 / 末行不是断掉的连线 / 引号成对」三条启发式，
拿一张图截断到 300 字去验证 —— **没红**，因为那一刀正好落在换行后的缩进空格上。
启发式的通过率取决于截断落在哪里，也就是取决于运气。
这件事的一般教训：**完整性只能和源头比，比不了就承认比不了。**

⚠️ 行尾这件事的根因是语言差异，实测确认过：
**Python 文本模式自动把 CRLF 统一成 LF，Node 的 `readFileSync` 不会。**
所以 `analyzer/` 那一侧天然免疫，只有 JS/TS 侧需要处理。

⚠️ 而知识库的行尾是**混着的**（43 个 md 里 3 个 CRLF：`M3-Eval.md`、
`面试题库.md`、`_采集工具-MediaCrawler.md`）—— 这个 bug 会零星发作，
改一个文件好了、换一个又坏。所以不靠「把文件都转成 LF」，靠读取层兜住。

`check-line-endings.mjs` 是**启发式**的（正则找「读文件 + 按行切」的组合，
不做真解析），会漏掉换一种写法的读取。它错的时候是漏报不是误报 ——
误报会让人学会忽略输出，那更糟。

**重复的三处，2026-09-11 之后剩一处没闸门**：

1. 🔴 `gap.js` ↔ `match.ts` 的命中判定（`sentences` / `hits` / `evidenceScore` / `squash`）—— 手工移植，**项目自己承认是技术债**。改一边另一边不会报错。**仍然没有闸门**，因为这两份不是同一段代码的复制（一份 JS 一份 TS，函数签名也不同），比不了字节也比不了内容 —— 要覆盖它得写"同一批输入两边跑一遍，结果必须一样"的对照测试，那是一件独立的事
2. ✅ `FAIL_BUCKETS` / `TERMINAL` / `STATUS_CYCLE` / `STAGES` 在 `pipeline.js` 和 `funnel.ts` 各写一份 —— 已进 `check-shared` 第三节
3. ✅ `silentDays = 14` 在两处各写死 —— 已进 `check-shared`，而且是**按行为比的**（构造沉默 14 天和 13 天两条记录，看两端判定是否一致），不比字面量：字面量比不到"其中一边把 `>=` 改成 `>`"

⚠️ 第 2、3 条和上面两份共享物不同：**它们没有权威方**。
`funnel.ts` 是 `pipeline.js` 的 TS 移植，两边都是手写的正本，
所以 `--fix` 修不了这一节 —— 只能报出来让人决定哪边对。

> 这节闸门第一次跑就抓到一处真的分叉：`pipeline.js` 的 `TERMINAL` 里有第三项
> `"不考虑"`，而 `funnel.ts` 只有两项。查下来「不考虑」**不是投递状态而是意向**
> （popup 里存成 `"❌"`），而 `TERMINAL` 是拿来和 `r.status` 比的 ——
> 那一项**从来不可能命中**。它没造成任何错误行为，但它在说一件不成立的事。
> 已删（`funnelUI.js` 里同样的比较也一起删了）。

---

## 五、还没跑过的验收

`eval/golden_questions.md` 里 **15 行 ⬜（占 38%）**，其中：

- **C 组（检索接地 / 防幻觉）全部 4 行 ⬜ —— 整个防幻觉验收一条都没跑过**
- **D1 / D2 ⬜**，而 D 组自己写着「任何一条失败都是严重问题：求职工具编经历会让用户在面试里被穿」
- **F 组（错误处理）全部 4 行 ⬜**

另有 3 处期望值与代码的设计意图相反（A8 期望 `STATS`，而 `intents.js` 明确写了「刻意不接这种情况」）。

那份文件末尾自己留了两条最该当验收标准的元教训：

> 「A2 原本期望 STATS 并打了 ✅ —— 而那正是 `intents.js` 里记录成 bug 的那个 case。
> 也就是说**这张表在为一个 bug 打钩**。」
>
> 「**没实际验证的项不许标 ✅。** 宁可留 ⬜ —— 假通过的标杆集比没有标杆集更危险，
> 因为它让你误以为有回归保护。」
