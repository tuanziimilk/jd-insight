# 工程设计说明 · JD Insight + Career OS

> 这份文档写给两类读者：想了解这套系统怎么做的人，和想知道「做的时候在权衡什么」的人。
> 它不是使用说明（那在各自的 README），而是**决策记录**：每个决定的理由、代价，
> 以及什么条件下该反悔。

---

## 0. 一句话

**专为 AI 产品经理 / AI Agent 方向做的**求职情报系统：Chrome 扩展在招聘网站上采集 JD，
工作台把这些 JD 和「我的能力现状」放在一起做匹配分析，
中间用 Supabase 打通，跨设备一致。

不是「AI 帮我找工作」——是**把找工作过程中产生的数据变成可查、可比、可复盘的东西**。

### 0.1 为什么是「专为一个方向」，而不是通用工具

这不是定位话术，是**能力边界的如实声明**：

整套系统的核心资产是那份 26 项的技能词典（`career-web/src/data/skills.json`），
它按一份 AI 产品经理简历校准过三轮。拿 7 条真实 AI 产品 JD 做过覆盖扫描
（"找出一条技能都没命中的句子"），**AI/Agent 那一侧零漏判**，
剩下的盲区全在通用产品能力那侧——补了两处（To B、抽象/拆解），
故意不补一处（行业背景，理由见 §3.13）。

反过来说：非 AI 岗位的 JD 在这套词典里几乎无对应项。
一条财务经理的 JD 会命中「跨部门推动」「0→1」这种谁都写的项，
然后给出一张"你缺 RAG"的缺口表。**那张表看起来完全正常，但毫无意义——
它比报错更糟，因为报错你会去查，一张像样的表你会去信。**

所以词典里带了 `domain.coreSkills`，`gap.js` 用它做领域判定：
一条 JD 的正文里一个 AI 核心项都没命中，就当领域外**排除并明说**，
不硬算。判定用「成分」而不是「数量」——数量判不出方向
（非 AI 岗也能命中五六项通用能力），成分才行。

知识库那一侧（M1-RAG / M2-Agent / M3-Eval / M5-大模型八股 / M8-AI产品落地能力）
本来就是同一个方向，和词典共用一套概念。三个部分是一致的，
只是**在 v2.2 之前对外的自我介绍还在装通用**。

---

## 1. 为什么是三个部分，而不是一个

| 部分 | 技术栈 | 负责什么 | 为什么不能合并进别的部分 |
|---|---|---|---|
| **jd-insight**（Chrome 扩展，MV3） | 零构建纯 ESM | 在招聘网站页面上采集；本地问答 | 只有扩展能读到你正在看的那个页面 |
| **Career OS**（工作台） | Vite + React + TS | 匹配分析、漏斗、学习进度、简历管理 | 需要构建系统（PDF/Word 解析要打包依赖）；也需要一个能发给别人看的 URL |
| **Supabase** | Postgres + RLS | 唯一的持久化真相 | 两个客户端都要写，必须有个中间层 |

### 这个拆分是取舍，不是技术限制

扩展**技术上可以**装下整个工作台（MV3 允许完整的扩展页面，存储也够）。不合并的代价对比：

- **零构建原则**：扩展现在改一个文件、浏览器里点一次重载就生效。合并意味着每次改动都要走一遍打包。这是最贵的一条，而且是工作流成本而非技术成本。
- **体积**：PDF 解析（pdfjs 483KB + worker 1.2MB）和 Word 解析（mammoth 499KB）在工作台里是按需加载的独立 chunk；进了扩展就是整包更新，每个用户每次都付。
- **可分享**：网页能发链接、手机能打开；扩展页不行。

反过来，如果目标变成「装一个东西就全都有」，合并是可行路径。这条记在这里，免得以后把「当时的取舍」误当成「技术上不行」。

---

## 2. 数据流

```
招聘网站页面
   │  content.js：只读已渲染的 DOM
   ▼
chrome.storage.local ──── popup / sidepanel（本地问答、打标）
   │
   │  syncSupabase.js（手写 REST 客户端，不用官方 SDK）
   ▼
Supabase（career_jds / career_status_history / career_profile /
          career_learning_progress / career_question_practice /
          career_activity / career_deleted_jds）
   │
   ▼
Career OS 工作台（读写同一份数据）
```

单向的地方刻意保持单向：**简历**的权威副本在云端（工作台写），扩展只拉不推——
双向同步需要冲突解决，而这里没有任何场景值得付那个复杂度。

---

## 3. 工程决策记录

每条都是「问题 → 做法 → 代价 → 什么时候该反悔」。

### 3.1 采集：抓不准就留空，绝不猜

薪资是这套系统里唯一会被拿去排序和比较的数字。所以定了一条硬规则：
**错一位数比空着糟得多**。

具体表现：
- 页面上两处薪资对不上（DOM 里一个值、meta 里另一个值）→ 留空并标注冲突，不二选一
- 抓不到公司名 → 明确写「公司名未抓到」，不留空（留空看起来像"这家公司没名字"）
- 字段提取限定在详情面板范围内，不做全局回退

**为什么这条值钱**：踩过一次真实的坑——`getJobId` 有一个 `|| document.querySelector()` 的全局兜底，
在详情页上抓到了页脚「相似职位」里的第一个推荐岗位，结果把 A 岗位的正文存到了 B 的主键上。
内容是 A、链接是 B，之后会被 B 覆盖。**静默脏数据，比抓不到严重得多。**

### 3.2 反爬字体：现场推导映射，不硬编码

招聘网站把薪资数字换成私有区码位（U+E000–U+F8FF），靠自带字体渲染成数字，
文本层面读到的是 `-K·薪`。

做法：**每次在当前页面现场推导映射**——那个字体自带正常的 ASCII 数字字形，
于是把私有区字符和 `0`–`9` 用同一个字体画到 canvas 上比像素。

- 不硬编码映射表：硬编码最省事也最危险，网站换一次字体子集，表还在、结果全错，而且错得像真的
- 归一化必须保持宽高比（letterbox）：拉伸到正方形会丢掉宽高比，而 `1` 和 `9` 的主要区别就是它（实测拉伸版把 1/9 判反）
- 必须加「一一对应」约束求全局最优，不能各自取最近邻（实测有 3 个字形不是各自的第一名）
- **验收是语义的**：拿整页几十条解码结果去过薪资合理性（月薪 1k–300k、月数 12–24、下限≤上限），合理率 <85% 或说不通 >1 条就整个放弃还原

最后一条是关键。单字置信度不能当验收标准，因为错的排列会产出 `95薪` 这种不可能的值——
那才是有牙齿的检验。还原出来的薪资在界面上单独标「字形还原」，万一映射错了能一眼认出来源。

**什么时候该反悔**：如果网站改成图片渲染薪资，这条路直接失效，回退到手工补录。

### 3.3 检索：先建评测集，再改算法

这一条是整套系统里最有代表性的工程实践。

**问题**：检索挑错了 JD，模型会认认真真地基于错的上下文编一段合理的话——错得看不出来。
而在有评测集之前，「检索变好了」只能靠感觉。

**做法**：`scripts/eval-retrieve.mjs`——12 条构造语料 + 10 条人工标注用例，
每条标注写明依据。指标：recall@k、MRR、噪声条数。

实测三个版本（k=3）：

| 版本 | recall@3 | MRR | 全对用例 |
|---|---|---|---|
| A 原版（TF + 字段加权，无 IDF） | 86.7% | 1.000 | 7/10 |
| B 加 BM25F + IDF | **85.0%** ↓ | 1.000 | 6/10 |
| C 再加短语级查询扩展 | 88.3% | 1.000 | 7/10 |

**两个和直觉相反的结果，都是评测集抓出来的**：

1. **单独加 IDF 反而变差。** 12 条语料下 IDF 很噪声，而原版那个全局长度归一其实在起作用。真正有效的是查询扩展。
2. **第一版查询扩展把扩展词也拆成了 2-gram，评测立刻掉到 85%。** 问「有没有 B 端或者 SaaS 方向的产品岗」时，`SaaS` 命中「后台系统」这个技能，扩展出「内部工具 / 效率工具 / 运营工具」，拆成 bigram 后通用的「工具」命中了一个**兼职岗**的「内部AI工具链」，把它顶到第一名。改成**短语整体匹配 + 只看在不在（不看词频）**才转正。

没有这个评测集，第 2 版会被直接提交并声称改进了。

**同时必须说清测量的局限**：10 条用例下 1 条翻转就是 3–6 个点，A 和 C 差 1.6 个点
**不足以称为显著**。这一轮真正的产出是评测集本身和它抓到的那个回归，不是那 1.6 个点。

### 3.4 为什么还没上向量检索

不是「不做」，是**条件还没到**。条件写在 `extension/lib/retrieve.js` 头部：

- 语料超过约 500 条（词典覆盖不住长尾），或
- 评测集里出现「同义词典也补不上」的失败（跨语言 / 跨领域改写），或
- recall@3 跌破 80%

现在两个失败用例都是同义词缺口（问 `RAG` 但 JD 里写的是「知识库 / 召回」），而共享的技能词典能补。
代价对比差一个数量级：向量要付每条 JD 一次 embedding 调用、浏览器里维护索引、
**必须先配好 Key 才能检索**（冷启动体验变差）；查询扩展只要一个已经存在的词典文件。

判断只认评测数字，不认「感觉不够智能」。

### 3.5 统计类问题不进模型

「有几条要求 Discord」「薪资分布怎样」这类问题走确定性计算，不进模型。
理由：模型数不准，而这类答案要能复现。

**但这条踩过一个坑，值得记**：意图路由把两类词混在一个正则里——

- **量词**（几条 / 占比 / 覆盖率 / 分布）需要一个宾语，抠不出关键词就等于不知道要数什么
- **概览词**（统计 / 汇总 / 一共）本身就是在要那张全库概览表，没宾语是正常的

于是问「这批 JD 里最高频的能力要求是什么？按覆盖率排」，因为命中「覆盖率」被判成统计意图，
回了一张固定的全库统计表（JD 总数 / 不同公司 / 城市分布）。
**那张表没有一个字回答了问题，却带着「不经模型，直接算，结果可复现」的口气——比明说「我不知道」更糟。**

拆开之后：量词 + 抠得出关键词 → 确定性计算；只有量词、抠不出宾语 → 不硬答，交给检索 + 模型。

### 3.6 缺信息就追问，不基于空气回答

简历诊断需要「简历」这个槽位。没有就弹卡片要，用户也可以跳过——
那时**明确告诉他「结论会弱很多」**，而不是假装答得很好。

涉及事实性数字的改写走人工确认（HITL）。

### 3.7 共享物：一份权威 + 机器校验

扩展是零构建纯 JS，工作台是 Vite + TS，两边没有共享包的机制。
可选项只有「各写一份」和「一份权威 + 复制 + 机器校验」。

前者的结局是某天同一个输入在两端得到不同结果，而这种不一致只在数据对不上时才被发现，
极难定位。所以选后者，并且**把校验挂进 build**：

- 薪资解析 `salary.js`：权威在扩展，工作台原样复制，要求**字节一致**
- 技能词典 `skills.json`：权威在工作台（校准记录在那边），扩展那份是**生成**的 ESM 模块，要求内容一致

生成而不是直接 `import` JSON 的理由：JSON 模块导入需要 import attributes（Chrome 123+），
一旦用户浏览器不支持就是整个侧栏在模块加载阶段失败——
而这个风险在开发机上验证不了（Node 支持 ≠ 用户的 Chrome 支持）。

校验的有效性是实测过的：故意改坏词典后 `npm run build` 退出码为 1。
**一道不能让构建失败的校验就是装饰品。**

### 3.8 删除必须被正向记录，不能靠"不存在"推断

两个客户端都能删记录。如果删除只表现为「本地少了一条」，
下次同步时另一端会把它当成"对方还没有"而重新推回来——记录复活。

做法是两层墓碑：本地 `deletedKeys` + 云端 `career_deleted_jds`。
比较 `deleted_at` 与记录 `ts` 来允许「删掉之后重新采集」这个合法场景。

### 3.9 权限最小化 / Key 由用户自己持有

- API Key 只存在用户浏览器本地，只发往用户自己填的 base_url。**项目没有任何服务器。**
- 代价说明白：能物理接触这台电脑的人可以读到它
- 站点权限按需申请（`optional_host_permissions` + 运行时 `chrome.permissions.request`）
- Supabase 的 anon key 可以公开——权限完全由数据库的 RLS 决定，每张表都是 `user_id = auth.uid()`

### 3.10 迁移脚本必须可重跑

`schema.sql` 全文幂等（`create table if not exists` / `create or replace` /
`drop policy if exists` 后重建）。理由：一条 `42710`（对象已存在）会让整个脚本回滚，
而人对着一个"跑一半"的数据库是没法判断状态的。

### 3.11 无彩配色下的两条硬约束

界面是近乎无彩的（白/米白底、发丝边、主按钮近黑，没有品牌主色）。
这带来两个必须补的东西，否则会退化：

- 输入框聚焦不能只换边框颜色（纯黑强调色下看不出来），要加一圈 box-shadow
- 链接必须带下划线，不能只靠颜色区分

另外颜色只承担三件事：薪资状态、删除、字形还原来源。
「有颜色」本身就是信号，纯提示性的元素不占用它。

### 3.12 不用 LangChain（但确实有一层 harness）

意图路由 + 槽位追问 + 检索 + 上下文组装 + 用量统计，这些确实是 harness 该做的事。
自己写的理由：需要的部分很薄（约 300 行），而框架带来的抽象层在排障时是净负担——
这个项目里绝大多数 bug 是「取值取错了对象」，而不是「编排逻辑不对」。

---

## 4. 哪些东西是被测量过的

工程主张要有证据，否则只是主张：

| 主张 | 证据 |
|---|---|
| 检索质量 | `scripts/eval-retrieve.mjs`，recall@3 / MRR / 噪声，三版对比数字见 3.3 |
| 薪资解析两端一致 | `npm run check:shared`，字节比对，挂进 build |
| 字形还原可信 | 整页样本合理率 + 「说不通 ≤1 条」双门槛；对照页面截图逐位核对过 |
| 无障碍对比度 | 实算（非估算）13 项 × 浅深两套，含 WCAG 1.4.11 的 3:1 组件边界 |
| 模块可加载 | 14 个 ESM 模块严格解析检查（只认 SyntaxError，不把"模块找不到"当通过） |
| 删除三端同步 | 13 条墓碑测试 |

---

## 5. 明确还没做的部分

写出来是因为一份只讲优点的文档不可信。

- **Memory 只有短期**：对话历史只在内存里保留最近 6 轮，换会话即清空。没有长期记忆（标过的意向、确认过的能力缺口）。
- **Agent 层还很薄**：有 6 个意图 + 槽位 + HITL 标记，但没有工具调用。
- **能力缺口聚合还没做**：从 JD 正文聚合高频能力 × 简历 = 缺口清单——这是这套系统最该有的输出，目前还是空的。
- **统计器功能不全**：例如「薪资中位数」问不出来（没实现）。
- **评测集偏小**：12 条语料 / 10 条用例，不足以分辨小幅改动。
- **学习内容刻意不由模型生成**：只输出「缺哪些能力」，具体学习方案和资料由人整理导入。这是产品边界，不是待办。

---

## 6. 开源前必须处理的隐私项

⚠️ 这两个仓库现在包含个人数据，**直接公开会泄露信息**：

| 位置 | 内容 | 处理 |
|---|---|---|
| `career-web/src/data/skills.json` 的 `calibration` 字段 | 按真实简历校准的记录，隐含自评短板 | 摘掉或改成中性描述 |
| `career-web/src/data/modules.json`、`questions.json` | 从个人知识库导出的学习内容 | 确认不含公司名、内部系统名、个人短板原文 |
| `career-web/src/lib/demoData.ts` | 已是构造数据，但要确认没混入真实公司名 | 复核 |
| `.env.local` / Supabase URL 与 key | 不在仓库里（已确认），但要确认历史提交里也没有 | `git log -p` 搜一遍 |
| 采集到的 JD | 存在 `chrome.storage` 和 Supabase，不在仓库里 | 无需处理 |

另外：**JD 正文是招聘方的内容**。自用采集属于正常浏览行为，但把 JD 原文打包发布是另一回事，不要做。

---

## 7. 目录

- `jd-insight/extension/` —— Chrome 扩展（零构建）
  - `content.js` 采集｜`popup.js` 列表与打标｜`sidepanel.js` 问答与漏斗｜`options.js` 设置
  - `lib/` —— `salary.js` 薪资解析｜`glyphmap.js` 字形还原｜`retrieve.js` 检索｜`intents.js` 意图路由｜`llm.js` 模型调用与计费｜`syncSupabase.js` 云同步｜`pipeline.js` 漏斗计算｜`skills.js` 技能词典（生成）
- `jd-insight/scripts/eval-retrieve.mjs` —— 检索评测
- `career-web/` —— 工作台（Vite + React + TS）
  - `src/lib/` —— `types.ts` 数据契约｜`supabaseSource.ts` / `localSource.ts` / `demoData.ts` 三种数据源｜`match.ts` 匹配分析｜`resume.ts` 简历解析｜`funnel.ts` 漏斗
  - `supabase/schema.sql` —— 幂等建表脚本
  - `scripts/check-shared.mjs` —— 共享物校验（挂进 build）
