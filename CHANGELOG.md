# 变更记录

> 为什么有这份文件：用户的原话是「我提需求，你库库去做，具体做了什么我也不知道」。
> commit message 写得再详细，也需要一次 `git log` 才看得到，而且看不出**哪些需要你动手**。
>
> 所以每个版本固定三段：
> **改了什么** · **需要你复核什么** · **我没验证的部分**
>
> 最后一段是刻意的：**"写完了"和"跑过了"是两件事**，混在一起报告是这个项目
> 出过最多次的问题（详见 v2.2.0 的记录）。

---

## v2.3.0 · 透明化与维护入口（2026-09-10，进行中）

对应规划的 **P0**。起因是用户指出整个项目对他像黑箱，且不知道哪些东西需要人工维护。

### 改了什么

**P0.1 分析器加两道闸门**（`analyzer/analyze_jd.py`）

发现一个**已经在产生错误输出**的问题：`analyzer/config.py` 与 `config.example.py`
**逐字节完全相同**，`MY_PROFILE` 里躺的一直是模板占位（每条带「示例」二字），
而 `reports/jd_report.md` 把这些示例当成真实能力印出来、据此排出「你最该补 RAG / 对话式产品」。

原来的 `USING_EXAMPLE` 只判断「`config.py` 存不存在」—— 文件存在、内容是示例，
所以那句警告一次都没打过。判断依据改成「内容填了没有」：

- 新增 `placeholder_groups()` / `PROFILE_UNUSABLE`：自评全是占位时，**缺口排行整章拒绝输出**，
  并说清为什么和怎么修。刻意只认「示例」这个固定标记词，不做更聪明的启发式 ——
  那会误伤真实填写的简短自评，而误伤的表现是"明明填了却被拒"，比漏判更让人不想用。
- 新增 `MIN_SAMPLE = 10`：样本 < 10 条时缺口章**降级为只给原始计数、不排优先级**。
  报告头部本来就写着「样本少于 10 条时结论不稳」，但**那只是一句提示，代码没有执行它**
  ——两份历史报告的样本量是 4 条和 3 条，照样输出了带 🔥🔥 的排行。
- 覆盖率表的「我的现状」列遇到占位显示 `—（未填自评）`，不再把示例当事实印出来。

**P0.3 新增 `MAINTENANCE.md`** —— 把「哪些数字是 AI 推测的、错了会怎样、你怎么改」
全部摊开。20 项完全无界面、只能改代码的产物按紧迫度排好，含机器闸门覆盖了什么、
以及 `golden_questions.md` 里 15 行从未跑过的验收。

**P0.4 新增本文件。**

### 需要你复核什么

| # | 事项 | 怎么做 |
|---|---|---|
| 1 | **填 `analyzer/config.py` 的 `MY_PROFILE`** | 按 `GROUPS` 里的组名逐个填真实自评（🟢/🟡/🔴）。在填之前，缺口排行会一直拒绝输出。该文件被 `.gitignore` 排除，不进仓库 |
| 2 | `deepseek-v4-pro` 的计价 | 官方文档提到 **2026-09-14 起**切到 V4.1 Flash 计价 —— 已到期，需核对 `providers.js` |
| 3 | 通读一遍 `MAINTENANCE.md` | 特别是「26 项技能的 weight」那一行：它是**我按"这项对你这份简历有多核心"定的个人判断**，但输出时表现为客观排行 |

### 我没验证的部分

- `MAINTENANCE.md` 里的 20 项清单是**三份代码调查汇总**的，我自己逐个核对过其中约一半
  （阈值、价格、正则、评测集），另一半（`content.js` 的选择器、`funnel.ts` 的重复常量）
  只核到"确实存在两份"，没有逐字比对内容是否已经分叉。
- 分析器的两道闸门有行为测试（`analyzer/test_gates.py`，10 条断言，占位/误伤两个方向都测），
  且真跑过一遍确认生效。但**没有测"填了真自评 + 样本 ≥ 10 条"这条完整路径** ——
  那需要先有 10 条真实语料。

### 一处对我自己说法的更正

我最初还说「那份错报告回流进了知识库 `05-资源库/JD汇总报告.md`」。**这句是错的。**

实际核对：知识库那份报告里 18 条「我的现状」**全是真实自评**，一个「示例」都没有 ——
它是在 `config.py` 还有真实内容时生成的，之后 `config.py` 被重置回示例了。
受影响的只有本地那份 `reports/jd_report.md`（被 `.gitignore` 排除、无 git 历史）。

知识库那份的真实问题是**样本 3 条却排了 🔥🔥**，已在文件头加了 warning 标注，
并说明生成器已修但这份文件是当时的产物、需要重跑覆盖。

> 这个更正本身说明了为什么要做 P0：**我上一轮是靠转述子调查的结论下判断的，没有自己核。**
> "AI 说了一个听起来很具体的坏消息"和"这件事真的发生了"是两回事。

---

## v2.2.0 · 定位收窄到 AI 产品经理 / AI Agent（2026-09-10）

`6a1a9ec` · `f4ffe84`（career-web）

### 改了什么

- **把「专用」写成代码约束**：`gap.js` 加领域判定 —— 一条 JD 正文里一个 AI 核心项
  （rag/agent/workflow/prompt/dialog/eval/guardrail/llm-basic/multimodal）都没命中，
  就当领域外排除并明说。因为一条财务经理的 JD **确实会命中**「跨部门推动」「0→1」
  这种谁都写的项，然后给出一张"你缺 RAG"的表 —— **那张表看起来完全正常但毫无意义，
  比报错更糟：报错你会去查，一张像样的表你会去信。**
- 判定用**成分**不是**数量**：非 AI 岗位也能命中五六项通用能力，数量判不出方向。
- **修一个中英空格导致的漏判**：词典有 pattern `B端`，而真实 JD 写「B 端产品经验」，
  中文 pattern 走 `includes()` 整条漏掉。现在句子和 pattern 都做归一化：
  空格两侧只要有一侧是汉字就删掉，纯拉丁词之间保留（否则 `function call` 被压成
  `functioncall`，那是把一个漏判换成另一个）。
- 词典补两处（`scoping` 的抽象/拆成/归纳、`backend-system` 的 To B/企业服务/ERP），
  **刻意不加行业背景标签** —— 行业背景和学历年限一样是门槛不是能补的能力。
- 实测：词典盲区从 9 句降到 1 句（剩的那句就是刻意不覆盖的行业背景）。

### 需要你复核什么
- `domain.coreSkills` 那 9 项是我挑的，它决定一条 JD 是否被**整条丢弃**。
- 那 26 项的 `weight`（决定"先补哪个"的排序）。

---

## v2.1.x · UI 梳理与厂商目录（2026-09-10）

`50ef1eb` · `53b15ad` · `5ad2586` · `79d97cd` · `7fd75fc` · `348178a`（career-web）

### 改了什么
- **内置五家厂商目录**（OpenAI / Gemini / DeepSeek / 火山引擎 / 硅基流动 + 本地 Ollama，
  18 个模型），模型改原生下拉、**key 按厂商分开存**、价格带来源和抄录日期。
  查不到缓存命中价的 7 条留「未知」，不编。
- **设置页删掉三块冗余**：整个可编辑价格表（它把 18 个内置价全挡住了）、
  手填 JSON 的「附加请求参数」（换成一个「关闭思考模式」复选框，参数名按厂商内置）、
  Data 区的两张表。使用说明从首屏移到页尾折叠。
- **网页端登录改成账号密码**（原来只有邮件链接，而扩展早就用密码 ——
  也就是说密码早就存在，只是网页端自己不用它）。
- UI 梳理修了四处违反项目自己定的规则：`.tip` 11.5px（theme.css 写着连读中文 ≥12px，
  后面还跟着"别写成一个代码里没遵守的数"）、写死的圆角（token 只有 6px 和 4px）、
  legend 英文大写、以及一个我上一轮删表格 CSS 时**顺手弄坏的 `.mini` 孤儿类**。

### 需要你复核什么
- 18 条价格的 `asOf` 全是 2026-09-10 同一天抄的。
- `deepseek-v4-pro` 的 09-14 改价提醒。

---

## v2.1.0 · 能力缺口聚合 + 修安全洞（2026-09-10）

`46ab0e7` · `e229f33` · `3b9604a`

### 改了什么
- **能力缺口聚合**（`lib/gap.js`）：跨全库统计高频要求 × 简历，**完全不过模型**。
  这是「只给学习路数、不给学习方案」这条边界的代码化 —— 写在提示词里靠模型自觉不够，
  它一定会顺手推荐课程和书。
- **修一个真安全洞**：「我没做过 Discord 运营，帮我写一条」原来命中 `rule:kw`
  → 判成 ASK_JD → 走普通问答，而「绝对不许编造」这条硬规则**只写在 REWRITE 分支的
  提示词里** —— 红线的防护装在了这个请求永远不会经过的分支上。
  加 `GUARD` 意图：判定优先级最高、确定性拒答、不进模型。
- 新增 `scripts/eval-intents.mjs`（28 条断言）+ `scripts/check-syntax.mjs`。

### 这一版的事故（这是本文件最后一段存在的原因）

**我把情报台整个搞坏了两次，而且第一次报告成"已完成"。**

`answerGap()` 和 `answerGuard()` 里字符串拼接的 `\n` 落到磁盘上变成了**真换行** ——
那是 SyntaxError，整个 `sidepanel.js` 不加载。表现是页面长得完全正常
（静态 HTML 照样渲染）但没有任何东西被接线，用户报的是「设置页打不开」。

**而 `node --check` 全程报 OK** —— 它把文件按脚本解析，扩展里全是 ESM 模块。
我一直拿它当闸门，它对这类问题是**假通过**。

这是同一个坑的第四、五次（第五次就发生在我为它写的检查脚本里）。修法三处：
① 那两个函数改成数组 + `join`，没有转义字符；② 新增 `check-syntax.mjs` 做真 ESM 解析；
③ 新增 `guard.js` 给情报台和设置页加错误守卫（popup 早就有，这两页一直没有）。

**教训写进了流程**：涉及字符串字面量的 JS 只用编辑工具写，不用脚本生成。
以及"只验证了纯函数和 eval"不等于"这个功能能用"。

---

## v2.0.x 及更早

完整历史见 `git log`。关键节点：

| 版本 | 内容 |
|---|---|
| `4963f4b` | 检索层：**先建评测集，再改算法** —— 结论和预期相反（BM25F+IDF 单独上线让 recall 从 86.7 掉到 85.0，bigram 查询扩展同样是净退步）。两次都只有评测集能发现 |
| `626ddbd` | 新增 `ARCHITECTURE.md`（12 条工程决策 + 「哪些东西被测量过」+ 「明确还没做的部分」） |
| `4ab31c1` · `633d81a` | 还原被私有区字体挡住的薪资。⚠️ 第二个提交修的是「字形还原从来没生效过」的原因：`clean()` 里有一句删私有区字符，而接线写成了 `deglyph(clean(x))` —— 要还原的字符在还原之前就被删干净了 |
| `8a6884a` | 底色改米白，并按**实算对比度**修掉四处不达标（含 `--rule-strong` 1.58:1，而 WCAG 1.4.11 对 UI 组件边界要求 3:1 —— 这是我原本整个漏掉的一类） |
| `affd1a5` | 存 JD 抓鼠标停留的那条，并修掉 v1.3 一个把整页打瞎的 bug：`[class*='recommend']` 命中了 BOSS 列表页的**整页外壳**，于是每个字段都返回空 |
