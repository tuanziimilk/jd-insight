# 变更记录

> 为什么有这份文件：用户的原话是「我提需求，你库库去做，具体做了什么我也不知道」。
> commit message 写得再详细，也需要一次 `git log` 才看得到，而且看不出**哪些需要你动手**。
>
> 所以每个版本固定三段：
> **改了什么** · **需要你复核什么** · **我没验证的部分**
>
> 最后一段是刻意的：**"写完了"和"跑过了"是两件事**，混在一起报告是这个项目
> 出过最多次的问题（详见 v2.2.0 的记录）。

---

## v2.3.0 · 透明化与维护入口（2026-09-10）

**P0 已完成。**

对应规划的 **P0**。起因是用户指出整个项目对他像黑箱，且不知道哪些东西需要人工维护。

### 改了什么

**P0.5 + P0.6 新增 career-web 的「07 校准台」**（`src/pages/Calibration.tsx`）

两件事合成一页，因为它们有一条硬约束：`skills.json` 的 note 写着
「group 必须和 `career_profile.capabilities` 里的能力组名**逐字一致**」。

- **能力自评编辑**（P0.5）—— 调用早就实现好、却**零调用者**的 `updateCapabilities()`。
  在这之前 04 匹配整页依赖的自评只能去 Supabase 后台改 jsonb。
  **组名不给输入框，从词典派生** —— 手填一个错字就会静默少算一整组能力，
  而界面上看不出任何异常。这一整类问题就此消失。
- **技能词典体检**（P0.6，只读）—— 26 项的权重、patterns、校准记录全部摊开，
  并且**拿真实采集的 JD 现场跑一遍**：显示每一项命中了几条、命中的原句是什么、
  哪些 pattern 真被验证过（绿底）、哪些还只是猜的（白底）。
  词典本身仍然只能改代码（它是编译进产物的静态文件），
  但**"看不见"和"改不了"是两个问题，而看不见才是黑箱感的主要来源**。
- `match.ts` 导出 `SKILLS` / `DICT_META` / 新增 `auditSkill()`。
  体检刻意**复用已有的 `sentences`/`hits`/`evidenceScore` 判定**，不另写一套 ——
  体检的意义就是显示线上真实行为，一旦另写一套，它显示的是另一个算法的行为，
  比不显示更误导。顺带把正文门槛抽成 `usableText()`，因为体检的分母必须和
  `analyzeMatch` 完全同口径。
- Match 页无自评时的提示改成链到校准台。原来那句话**告诉你数据在哪张表，
  但不给你填的地方** —— 那时确实没有。

自己在浏览器里跑出来、当场修掉的两个问题：

1. 标题写「拿 10 条 JD 实跑」而每行分母是 `/5`（10 条里只有 5 条有正文）——
   **标题和表格自相矛盾**。口径不一致会直接让人不信这张表里的任何数字。
   改成和 `gap.js` 同样的说法：「10 条 JD 里 5 条有正文参与统计」，并说明为什么。
2. `auditSkill()` 原来在渲染里逐项调用 —— 于是**在自评输入框里每敲一个字，
   26 项 × 10 条 JD × 每项十几个 pattern 全部重跑一遍**。改成 `useMemo` 只依赖 jobs。

**顺手补上了 career-web 的第一个 eval**（`scripts/eval-capability-note.mjs`，27 条断言）

`composeNote` / `splitNote` 抽到 `src/lib/capabilityNote.ts`。为什么值得单独测：
`career_profile.capabilities` 存的是 `{ 组名: note }`，**等级没有单独的列** ——
它是从 note 的 emoji 前缀反解出来的。写回时漏了前缀，后果是
**所有等级静默变成「未评估」**：不报错，只是 04 匹配的覆盖率变成 null，整页数据消失。

最关键的一组断言是**往返一致**（compose 之后再 split 必须拿回原样）——
单独测任一方向都可能"两边一起错"而测不出来。另有一组核对
「和 `supabaseSource.getCapabilities()` 的反解正则同口径」，
不然会出现"读出来是 🟡、编辑器里显示未评估"这种撕裂。

已挂进 `npm run build`（和 `check:shared` 同样的道理：不在 build 里跑的检查迟早不跑），
并且**故意改坏验证过它真的会让 build 失败**（exit 1）—— 一个从不报错的检查等于没有检查。

写这个脚本时我自己踩了一次：第一版以为 Node 跑不了 `.ts`，写了段正则把函数体抠出来
剥类型再求值 —— 那条剥参数类型的正则把返回值对象字面量里的 `{ level: "", … }`
也剥了，成了 `level is not defined`。**它不是不工作，是在你以为测的是源码时，
实际测的是一个被改坏的副本。** Node 22 本来就能直接 import `.ts`，
有现成的正确工具就别自己拼一个。

**P0.1 分析器加两道闸门**（`analyzer/analyze_jd.py`）

发现一个**已经在产生错误输出**的问题：`analyzer/config.py` 与 `config.example.py`
**逐字节完全相同**，`MY_PROFILE` 里躺的一直是模板占位（每条带「示例」二字），
而 `reports/jd_report.md` 把这些示例当成真实能力印出来、据此排出「你最该补 RAG / 对话式产品」。

原来的 `USING_EXAMPLE` 只判断「`config.py` 存不存在」—— 文件存在、内容是示例，
所以那句警告一次都没打过。判断依据改成「内容填了没有」：

- 新增 `placeholder_groups()` / `PROFILE_UNUSABLE`：自评全是占位时，**缺口排行整章拒绝输出**，
  并说清为什么和怎么修。刻意只认「示例」这个固定标记词，不做更聪明的启发式 ——
  那会误伤真实填写的简短自评，而误伤的表现是"明明填了却被拒"，比漏判更让人不想用。
- 新增 `MIN_SAMPLE = 10`：样本 < 10 条时缺口章**降级为只给原始计数、不排优先级**。
  报告头部本来就写着「样本少于 10 条时结论不稳」，但**那只是一句提示，代码没有执行它**
  ——两份历史报告的样本量是 4 条和 3 条，照样输出了带 🔥🔥 的排行。
- 覆盖率表的「我的现状」列遇到占位显示 `—（未填自评）`，不再把示例当事实印出来。

**P0.3 新增 `MAINTENANCE.md`** —— 把「哪些数字是 AI 推测的、错了会怎样、你怎么改」
全部摊开。20 项完全无界面、只能改代码的产物按紧迫度排好，含机器闸门覆盖了什么、
以及 `golden_questions.md` 里 15 行从未跑过的验收。

**P0.4 新增本文件。**

**P0.7 阈值登记**（`career-web/src/data/thresholds.json` + `scripts/check-thresholds.mjs`）

18 项影响结论的阈值全部登记：值 / 在哪个文件 / 依据是什么 / **被测量过没有** /
改错了会怎样。校准台加了一节显示它，按"没依据"的项标底色。

统计本身就是答案：**有正当依据 3 项、方向有理由但数字是拍的 9 项、完全没依据 6 项**。
（唯一真有外部依据的是 BM25 的 `k1=1.2` / `b=0.75`，论文标准值。）

⚠️ **刻意不把常量搬到一处共享文件。** 直觉做法是那样，但这个项目已经有三份
共享物了，其中 `gap.js ↔ match.ts` 那段命中判定**项目自己承认没人看住** ——
再加一份就是第四个漂点，而且那是一次要动六七个正在工作的文件的重构。
改成「常量留在原地 + 登记表 + 机器逐字核对」：改了代码没改表、或反过来，
都让 build 失败。**所以这张表不是文档，是契约。**

校验刻意用「整行逐字匹配」而不是解析出数值再比对 —— 写个小解析器要处理
JS/TS 两种语法、对象字面量、默认参数，而它的失败方式是**在你以为校验了的
时候其实没校验**。逐字匹配很笨，但它错的时候是报错不是静默放过。
（这个教训来自同一天：我给 eval-capability-note 写的正则剥类型器把返回值
对象字面量也剥了。）已故意改坏一个阈值验证过 build 会失败。

顺带修了一处**注释和代码差一个量级**的不一致：`match.ts` 的 `evidenceScore`
里注释写「30~120 字最合适」而代码是 `>= 12`。这类不一致最坏 ——
下次改的人会照注释去改代码，或照注释去理解行为，两种都错。

**P0.8 修 ROADMAP / PRD 的自相矛盾**

原计划只修一条（云端同步），实际发现**两条**：

1. 「云端同步 / 多设备 → 永远不做」，理由是"要有服务器就要承担别人的数据"。
   而 v2.15 已经做了 —— 实际做法是**用户自带 Supabase 项目**，我这边没有
   任何服务器。原理由假设了"我提供服务器"这一种实现方式，责任边界完全不同。
2. 「破解字体反爬 → 规避技术措施 → 永远不做」，而 `glyphmap.js` 已经实现了。
   这是用户明确要求的（我提过顾虑、用户重申后做的）。

两条都**留档修订而不是删行** —— 一份说着"永远不做"而代码已经做了的路线图，
比没有路线图更误导：读它的人（包括三个月后的自己）会据此判断项目边界。

同时标注了 v2.1「让结论能被验证」的真实状态：它被标成「最该先做」，
但**只完成了第 1 项的一半** —— 四套确定性评测测的都是"不过模型"的部分，
而那一项原本说的是 golden_questions 里要过模型的问题批量跑，
那份文件现在还有 15 行从没跑过（C 组防幻觉整组、D1/D2 编经历红线）。
第 2、3、4 项一项没动，已排进 P1。

ROADMAP 末尾接上 P0–P6 序列，避免两份路线图并存；
贡献方向那一条也改了 —— 它原来指向 `analyzer/config.example.py` 的 `GROUPS`，
而那是**已经分叉的旧词典**，权威源是 `skills.json`。

### 需要你复核什么

| # | 事项 | 怎么做 |
|---|---|---|
| 1 | **填 `analyzer/config.py` 的 `MY_PROFILE`** | 按 `GROUPS` 里的组名逐个填真实自评（🟢/🟡/🔴）。在填之前，缺口排行会一直拒绝输出。该文件被 `.gitignore` 排除，不进仓库 |
| 1b | **在 07 校准台填一次能力自评** | 云端那份（`career_profile.capabilities`）。填完 04 匹配分析才算真正可用 —— 在此之前它整页算不出东西。需要先登录云端数据源 |
| 1c | **顺手看一眼词典体检里命中 0 的那几项** | 命中 0 要么这批 JD 真的不要，要么 patterns 写漏了。点开能看到它想匹配什么 —— 这是判断词典准不准最快的方式 |
| 2 | `deepseek-v4-pro` 的计价 | 官方文档提到 **2026-09-14 起**切到 V4.1 Flash 计价 —— 已到期，需核对 `providers.js` |
| 3 | 通读一遍 `MAINTENANCE.md` | 特别是「26 项技能的 weight」那一行：它是**我按"这项对你这份简历有多核心"定的个人判断**，但输出时表现为客观排行 |
| 4 | **看一眼 07 校准台的「阈值登记」那一栏** | 18 项里有 6 项标着「没依据」、9 项「数字是拍的」。最该看的三个：`level-score-partial`（🟡 折算 0.5，直接换算成你看到的覆盖率百分比）、`field-weight`（检索的字段加权，决定模型看到哪 5 条 JD）、`evidence-lead-ratio`（0.12，决定引哪句原文当依据） |

### 我没验证的部分

- `MAINTENANCE.md` 里的 20 项清单是**三份代码调查汇总**的，我自己逐个核对过其中约一半
  （阈值、价格、正则、评测集），另一半（`content.js` 的选择器、`funnel.ts` 的重复常量）
  只核到"确实存在两份"，没有逐字比对内容是否已经分叉。
- 分析器的两道闸门有行为测试（`analyzer/test_gates.py`，10 条断言，占位/误伤两个方向都测），
  且真跑过一遍确认生效。但**没有测"填了真自评 + 样本 ≥ 10 条"这条完整路径** ——
  那需要先有 10 条真实语料。
- **07 校准台的保存路径没有真跑过。** 我在浏览器里验证的是演示数据源（只读），
  确认了渲染、体检的命中原句、pattern 高亮、展开折叠、以及四列不溢出（用几何量的，不是看截图）。
  但「切到云端 → 改自评 → 保存 → 04 匹配的覆盖率跟着变」这条链路需要登录，我没有你的凭据。
  **这一条要你自己验**，也是规划里给这一项定的验收标准。
- ~~校准台没有写自动化测试~~ —— **写完这段之后当场补上了**（见下）。
  剩下真正没测的只有"要登录"的那一段。

### 一处对我自己说法的更正

我最初还说「那份错报告回流进了知识库 `05-资源库/JD汇总报告.md`」。**这句是错的。**

实际核对：知识库那份报告里 18 条「我的现状」**全是真实自评**，一个「示例」都没有 ——
它是在 `config.py` 还有真实内容时生成的，之后 `config.py` 被重置回示例了。
受影响的只有本地那份 `reports/jd_report.md`（被 `.gitignore` 排除、无 git 历史）。

知识库那份的真实问题是**样本 3 条却排了 🔥🔥**，已在文件头加了 warning 标注，
并说明生成器已修但这份文件是当时的产物、需要重跑覆盖。

> 这个更正本身说明了为什么要做 P0：**我上一轮是靠转述子调查的结论下判断的，没有自己核。**
> "AI 说了一个听起来很具体的坏消息"和"这件事真的发生了"是两回事。

---

## v2.6.0 · P1.3 + P1.4 M10 纳入导入 + 脱敏第二道防线（2026-09-11）

这两项必须一起做，理由在上一轮就说过：M10 的正文里有自评和面试话术，
而当时的脱敏只查标题。**先做 1.4 才敢做 1.3。**

### 改了什么

**脱敏从两层变成四层。** 前三层在 `import-content.mjs`：

1. `REDACT` 替换词 —— 加了**在投的目标公司名和它的产品名**
2. `EXCLUDE_TITLE_PATTERNS` 整卡排除 —— 加了 `/面试怎么说/` `/写成作品/`
3. `DROP_LINE_PATTERNS` **行级丢弃**（新）—— 标题正当、正文夹自述的情形，
   按作者自己写的行首标记（`**和我的连接**` 这种）丢

第四层是新的重点：**`scanLeaks()` 对着产物扫，不看规则、不看来源，
只看最后落进 json 里有什么字**（`scripts/leaks.mjs`，词表 18 条，每条注明理由）。
扫到就**拒绝写文件** —— 不是 warning，因为这类内容一旦提交进公开仓库，
git 历史里就永远有了。

**为什么非要有第四层**：前三层的共同弱点是只拦我想到的情形。而两个已查实的
「靠运气挡住」说明想不到是常态 ——

- 题库里「→ 归 M9 图 5 Reliability（**你的短板**）」「（**头号短板**）」
  「以下是**你目前答不出**的题」，现在没进产物的唯一原因是它们在
  `## 📦 外部面经补充题` 那一节，而解析在 `## 通用必考题` 处就 break 了。
  **保护它们的是一个为了完全别的目的写的 break。**
- M10 第 1 节标题「逐条是什么」完全正当，正文却夹着「**和我的连接**：
  我的 Obsidian 双链库…」，没露出来只是因为 `excerpt` 截断在 600 字。

**「本版已 review」签章**（`review-content.mjs` + `src/data/content-review.json`）。
导入脚本的文件头从第一版就写着「必须人工 review 产物，这是硬性红线」——
而这条红线的全部执行机制是跑完在终端打三行提示。**没有任何东西记录过它有没有
被执行过**，包括我自己这几轮跑的那几次导入，一次都没逐条看过。
现在它有了机械形式：逐条记内容哈希，变了就让 build 失败，
并且**指名道姓列出是哪几条变了**。

关键设计是最后这半句。一个只会说"内容变了请重新 review"的闸门，
会被一次 `--sign` 敷衍过去；而「新增 1 条、改了 1 条，就是这 2 条」
是真的看得完的量。

**M10 纳入导入**（P1.3）：3 张卡（第 0、1、2 节）。第 3 节「面试怎么说」和
第 4 节「写成作品」整节不收 —— 那不是"这个领域有什么知识"，
是**我要怎么跟这家公司说话**，里面有逐字话术和"投递时附上这个"的动作计划。
顺带 `npm run content:import`（上一轮已加）+ 新增 `check:content` / `content:review`。

### 我 review M10 时抓到了一个真问题

这是脱敏最容易骗过自己的一种情形，所以单独记：

脱敏把公司名换成了「某目标公司」，那一行看起来已经处理干净了 ——

> **为什么 某目标公司 关心这些**：他们的**愿景原话**是「提升每个人获得信息的质量」
> 「未来屏幕的**交互界面由大模型实时生成、千人千面**」

**名字没了，但逐字的愿景原话还在。** 那两句话随便一搜就能定位到是哪家公司。
替换词典对付得了名字，对付不了**引文指纹**。

由此加了一条反向断言，它的思路和其他条正好相反：
**把替换词「某目标公司」本身也当成泄漏词**。它出现在产物里，
说明有一段围绕具名公司写的内容活了下来，只是名字被抹了。
判据不是"名字露没露"，是"这段内容是不是在讲某一家具体公司"。零误报。

### 每道闸门都验证过会红

- `guardLeaks`：拆掉第 3 层的一条行级过滤后重跑导入 → **退出码 1，拒绝写入**，
  而且报出来的正是上面那个引文指纹（命中「某目标公司」和「愿景原话」两条）
- `review-content.mjs`：先签一次章，然后往产物里偷加一张卡、改一张卡的正文 →
  **精确列出「新增 1 条：m1-c99」「内容变了 1 条：m1-c3」**
- `eval-leaks.mjs`（新，27 条断言）：13 条**从知识库逐字抄来的**真实泄漏原文
  必须被抓、11 条正当知识内容必须不被抓。
  第二组不是凑数 —— 词表差点写进 `/面试/`，而十个模块里有 22 个标题带"面试"

### 需要你复核什么

1. ⚠️ **`npm run build` 现在是红的，这是故意的。** 卡在签章那一步：
   ```
   npm run content:review
   ```
   签之前请扫一眼 `src/data/modules.json` —— 尤其 M10 那 3 张卡。
   我自己看过它们（也是这么抓到引文指纹的），但**签章这个动作只能你做**：
   我签就等于我既写代码又给自己发合格证，那这道闸门就只是一道手续
2. **M10 里我判断"可以留"的三行自述，请你确认**（我倾向留，但这是你的信息）：
   - `m10-c2`：小节标题「🟢 A. 和我现有实践直接相通的（最该先看）」
   - `m10-c3`：「《A Philosophy of Software Design》通读一遍（最直接对话我的经验）」
   - `m10-c3`：「被问到时能说出我在看什么、看到了什么」
   都不含公司、不含短板，属于教学口吻里的"我"，所以我没丢
3. **题库那一节的自评仍在知识库原文里**（「你的短板」「头号短板」）。我**没有改你的笔记** ——
   那是你的文件。现在的状态是：就算解析范围哪天变了，第四层会拦住它们
4. 上一轮的迁移 SQL 预检还欠着

### 我没验证的部分

- **89 张卡我只逐条读了 M10 的 3 张**。其余 86 张和 47 道题，我依赖的是词表扫描
  （全绿）和"它们在上一轮之前就在产物里"。这正是签章这件事要你做的原因
- **`/Plan\s*[ABC]\b/` 这条词表规则可能误伤**。它现在没命中任何东西，
  但如果哪天笔记里出现「Plan B 方案对比」这种正当用法，它会误报。
  留着是因为「Plan C 主攻方向」这种写法在 `skills.json` 的 calibration 里确实存在
  （那是 P5 的头号隐私项）
- **没测过窄屏**，也没测过 M10 在数据源无进度记录时的状态显示（应该是"未读"，没实跑）

---

## v2.5.0 · P1.2 M9 的六张脑图进工作台（2026-09-11）

对应规划的 **P1.2**。这是 P4.1「学习脑图页」的前置 —— 在这之前，
那六张图是**脑图页唯一的内容源，而它们根本没进产物**。

### 改了什么

**图源从 Obsidian 搬进 `modules.json`**（`extractDiagrams()`）。

它们之前为什么不在：`excerpt()` 做两件事 —— ① 整段丢弃代码块
② 截断到 600 字。mermaid 块正好两头都挨着。所以图源单独抽一个字段，
**既不截断也不压空行**：半张 flowchart 不是"内容少了一点"，是语法错误。
（实测第 6 张图 694 字，走 excerpt 必被截。）

顺带确认了一件事，它让闸门可以写得很窄：**九个模块里只有 M9 有 mermaid**
（M1~M8 加 M10 共 18 个代码块，mermaid 0 个）。所以「M9 在、图却是 0」
一定是出了问题。

**页面上先只显示图源文本，不渲染成图**（`Learning.tsx` 的 `DiagramSource`，
默认折叠）。渲染 mermaid 要装约 2MB 的依赖，而那个依赖该怎么装、图怎么布局、
「学完点亮」长什么样，是脑图页要一起定的事。先让图源看得见它确实在，
比先装一个库更要紧 —— 折叠条上直接写着"暂未渲染成图"，不含糊。

**两道新闸门**（都挂 `npm run build`，都验证过会让 build 失败）：
`check-diagrams.mjs`（产物里的图源和知识库原文逐字相同）+
`guardDiagrams`（M9 解析出 0 张图就拒绝写入）。
另外补了 `npm run content:import`（P1.3 的一半 —— 导入脚本终于有 npm script 了）。

### 我在验证自己的闸门时失败了一次

按规矩，写完闸门要故意弄坏一个东西看它会不会红。我把一张图截断到 300 字 ——
**三条启发式断言（首行是图类型 / 末行不是断掉的连线 / 引号成对）全绿。**
因为那一刀正好落在换行后的缩进空格上：末行是两个空格，不以箭头结尾、
引号数 0、括号也平。

结论不是"再加几条启发式"。截断可以落在任何位置，启发式只覆盖它落在某些位置的
情形 —— **那种闸门的通过率取决于运气，不取决于正确性。** 所以改成和源头逐字比，
再拿同一个 300 字截断重试，这次红了。

代价要说清楚：知识库不在版本控制里，所以逐字比对这一节在源文件缺失的机器上
**跳过，并打印自己跳过了**。在那种机器上剩下的只有覆盖不全的结构性断言。
这是真的局限，不装作没有。

### 需要你复核什么

1. **六张图的折叠条**：学习路线页 → M9 → 点「看答案」→ 展开「图源（Mermaid，N 行）」。
   我核的是行数和字数跟源文件一致，**没有逐张读内容**
2. M10 仍未纳入导入（P1.3 剩下的一半）。**它不是两行代码的事**：
   M10 的 `## 1. 逐条是什么` 那张卡的正文里有「短期补不出深度（别装）」这类
   自曝短板的自评，而 `EXCLUDE_TITLE_PATTERNS` 只查标题、查不到正文。
   放着不管就是把自评推进一个可能给面试官看的公开产物 —— 所以它得和 P1.4
   （脱敏第二道防线）一起做，我没硬塞进这一轮

### 我没验证的部分

- **浏览器里只展开了第 1 张图**。六张的折叠条都在、字数都对得上（673/494/676/436/647/694，
  和源文件逐字比对通过），但只有第 1 张我肉眼看了渲染效果
- **没验证窄屏**。宽屏下 `pre` 没有横向溢出（实测 `scrollWidth - clientWidth = 0`），
  窄屏下会不会撑破布局没试
- mermaid 语法本身**没有被任何东西解析验证过**。逐字比对只能保证"和你在 Obsidian
  里写的一样"，不能保证"它能渲染"—— Obsidian 渲得出来就说明语法没问题，
  但那是你的证据，不是我的

---

## v2.4.0 · P1.1 题目 id 内容化（2026-09-11）

`7704f61`（career-web）+ 本轮

### 改了什么

**题目 id 从位置型改成内容派生**（`q-` + 题干的 FNV-1a 8 位十六进制）。
旧的 `m1-q7` 语义由「这条问答在 面试题库.md 里的出现次序」决定 ——
往中间插一道题，其后所有作答记录静默挂到别的题上。而"扩展题库"正是要做的事。

新增 `eval-question-ids.mjs`（21 条断言）。**这个测试先对着旧实现跑过、
确认它是红的**（三条 id 平移），再改的代码 —— 否则只能证明它对当前实现是绿的。

**顺带撞出一个更严重的：导入脚本一直在静默清空题库。**
`面试题库.md` 是 CRLF，而 JS 的 `.` 不匹配回车符，题库正则的 `(.+)$`
永远走不到字符串末尾，139 行一条都匹配不上。模块正则没锚 `$`，照常工作。
症状：模块 85 张卡全在、题目 0 条、不报错，然后把 questions.json 覆盖成 `[]`。
**任何时候跑一次导入就会清空题库**，只是之前没人重跑过。

修好后解析出 47 条，比旧产物多 8 条 —— 那 8 道题从没在工作台里出现过。
`modules.json` 同理多 1 张卡（M3，恰好也是 CRLF 文件之一）。

**两道新闸门**（都挂 build、都验证过会失败）：
- `guardShrink`：产出条数比已有产物少一半以上就拒绝写入。它防的不是 CRLF，
  是「解析悄悄失效 → 覆盖写 → 数据没了」这个模式本身。判据用"和已有产物比"
  而不是写死下限 —— 写死的下限在内容真的很少时会误拦。
- `check-line-endings.mjs`：查 Node 侧「读文件 + 按行切分」的地方有没有做行尾统一。

**全项目 CRLF 扫描**（本轮补做）：14 处按行切分逐个看过，
只有 `import-content.mjs` 一处真有问题，其余要么有 `.trim()`（SSE 流解析、
markdown 表格解析、popup 标题），要么 split 里已经带了回车符（`gap.js` / `match.ts`），
要么显式写了「斜杠 r 问号 斜杠 n」那种两种行尾都认的切分（`Match.tsx`）。

⚠️ 根因是语言差异，实测确认：**Python 文本模式自动统一行尾，Node 不会。**
所以 `analyzer/` 天然免疫。而知识库行尾是**混着的**（43 个 md 里 3 个 CRLF），
这个 bug 会零星发作 —— 所以不靠"把文件转成 LF"，靠读取层兜住。

> **写这一节时我自己又踩了一次同一个坑。**
> 上一轮我承诺过「涉及字符串字面量的 JS 只用编辑工具写，不用脚本生成」——
> 然后这次用脚本生成**Markdown**，里面描述 `\r` 的地方全部变成了真的 CR 字符，
> 文档里出现了三处空白的反引号对。
>
> 教训要扩大：**不是「JS 不能用脚本生成」，是「任何含转义序列的文本都不能」。**
> 而且这次特别讽刺 —— 写的正是一节关于「不可见字符导致静默故障」的说明。

### 需要你复核什么

| # | 事项 |
|---|---|
| **1** | **跑 `career-web/scripts/out/migrate-question-ids.sql`**。我跑不了（没有你的登录凭据，而且那是你云端的数据）。文件里加了**第 1 步预检查询** —— 先单独跑那一句，返回 0 行就说明你还没标记过任何题，**整个迁移可以跳过**；> 0 再跑第 2 步 |
| 2 | 那 8 道新题现在会出现在 03 题库页 |

### 我没验证的部分

- **迁移 SQL 没在真实数据库上跑过。** 映射本身逐条验过（39 条全覆盖、
  题干一一对应、零孤儿），但"执行后作答记录仍挂在原题上"要你跑完才算数。
- ⚠️ 迁移映射的锚点是**题干文本**。我第一版拿「当前文件里第 N 条」配对 ——
  而数据库里的旧 id 是上一版文件的顺序算的，中间加过题，两套顺序对不上。
  按位置配等于把记录搬到别的题上，**正是这次要修的 bug，换成由迁移脚本来犯**。
  eval 里加了三条断言钉住，其中一条专门反证"按位置配确实会错"。
- `modules.json` 只核了数量（85→86，仅 M3 +1）和无残留回车符，**没有逐张卡比对内容**。
- `check-line-endings.mjs` 是启发式的，会漏掉间接调用的读取写法。

---

## v2.2.0 · 定位收窄到 AI 产品经理 / AI Agent（2026-09-10）

`6a1a9ec` · `f4ffe84`（career-web）

### 改了什么

- **把「专用」写成代码约束**：`gap.js` 加领域判定 —— 一条 JD 正文里一个 AI 核心项
  （rag/agent/workflow/prompt/dialog/eval/guardrail/llm-basic/multimodal）都没命中，
  就当领域外排除并明说。因为一条财务经理的 JD **确实会命中**「跨部门推动」「0→1」
  这种谁都写的项，然后给出一张"你缺 RAG"的表 —— **那张表看起来完全正常但毫无意义，
  比报错更糟：报错你会去查，一张像样的表你会去信。**
- 判定用**成分**不是**数量**：非 AI 岗位也能命中五六项通用能力，数量判不出方向。
- **修一个中英空格导致的漏判**：词典有 pattern `B端`，而真实 JD 写「B 端产品经验」，
  中文 pattern 走 `includes()` 整条漏掉。现在句子和 pattern 都做归一化：
  空格两侧只要有一侧是汉字就删掉，纯拉丁词之间保留（否则 `function call` 被压成
  `functioncall`，那是把一个漏判换成另一个）。
- 词典补两处（`scoping` 的抽象/拆成/归纳、`backend-system` 的 To B/企业服务/ERP），
  **刻意不加行业背景标签** —— 行业背景和学历年限一样是门槛不是能补的能力。
- 实测：词典盲区从 9 句降到 1 句（剩的那句就是刻意不覆盖的行业背景）。

### 需要你复核什么
- `domain.coreSkills` 那 9 项是我挑的，它决定一条 JD 是否被**整条丢弃**。
- 那 26 项的 `weight`（决定"先补哪个"的排序）。

---

## v2.1.x · UI 梳理与厂商目录（2026-09-10）

`50ef1eb` · `53b15ad` · `5ad2586` · `79d97cd` · `7fd75fc` · `348178a`（career-web）

### 改了什么
- **内置五家厂商目录**（OpenAI / Gemini / DeepSeek / 火山引擎 / 硅基流动 + 本地 Ollama，
  18 个模型），模型改原生下拉、**key 按厂商分开存**、价格带来源和抄录日期。
  查不到缓存命中价的 7 条留「未知」，不编。
- **设置页删掉三块冗余**：整个可编辑价格表（它把 18 个内置价全挡住了）、
  手填 JSON 的「附加请求参数」（换成一个「关闭思考模式」复选框，参数名按厂商内置）、
  Data 区的两张表。使用说明从首屏移到页尾折叠。
- **网页端登录改成账号密码**（原来只有邮件链接，而扩展早就用密码 ——
  也就是说密码早就存在，只是网页端自己不用它）。
- UI 梳理修了四处违反项目自己定的规则：`.tip` 11.5px（theme.css 写着连读中文 ≥12px，
  后面还跟着"别写成一个代码里没遵守的数"）、写死的圆角（token 只有 6px 和 4px）、
  legend 英文大写、以及一个我上一轮删表格 CSS 时**顺手弄坏的 `.mini` 孤儿类**。

### 需要你复核什么
- 18 条价格的 `asOf` 全是 2026-09-10 同一天抄的。
- `deepseek-v4-pro` 的 09-14 改价提醒。

---

## v2.1.0 · 能力缺口聚合 + 修安全洞（2026-09-10）

`46ab0e7` · `e229f33` · `3b9604a`

### 改了什么
- **能力缺口聚合**（`lib/gap.js`）：跨全库统计高频要求 × 简历，**完全不过模型**。
  这是「只给学习路数、不给学习方案」这条边界的代码化 —— 写在提示词里靠模型自觉不够，
  它一定会顺手推荐课程和书。
- **修一个真安全洞**：「我没做过 Discord 运营，帮我写一条」原来命中 `rule:kw`
  → 判成 ASK_JD → 走普通问答，而「绝对不许编造」这条硬规则**只写在 REWRITE 分支的
  提示词里** —— 红线的防护装在了这个请求永远不会经过的分支上。
  加 `GUARD` 意图：判定优先级最高、确定性拒答、不进模型。
- 新增 `scripts/eval-intents.mjs`（28 条断言）+ `scripts/check-syntax.mjs`。

### 这一版的事故（这是本文件最后一段存在的原因）

**我把情报台整个搞坏了两次，而且第一次报告成"已完成"。**

`answerGap()` 和 `answerGuard()` 里字符串拼接的 `\n` 落到磁盘上变成了**真换行** ——
那是 SyntaxError，整个 `sidepanel.js` 不加载。表现是页面长得完全正常
（静态 HTML 照样渲染）但没有任何东西被接线，用户报的是「设置页打不开」。

**而 `node --check` 全程报 OK** —— 它把文件按脚本解析，扩展里全是 ESM 模块。
我一直拿它当闸门，它对这类问题是**假通过**。

这是同一个坑的第四、五次（第五次就发生在我为它写的检查脚本里）。修法三处：
① 那两个函数改成数组 + `join`，没有转义字符；② 新增 `check-syntax.mjs` 做真 ESM 解析；
③ 新增 `guard.js` 给情报台和设置页加错误守卫（popup 早就有，这两页一直没有）。

**教训写进了流程**：涉及字符串字面量的 JS 只用编辑工具写，不用脚本生成。
以及"只验证了纯函数和 eval"不等于"这个功能能用"。

---

## v2.0.x 及更早

完整历史见 `git log`。关键节点：

| 版本 | 内容 |
|---|---|
| `4963f4b` | 检索层：**先建评测集，再改算法** —— 结论和预期相反（BM25F+IDF 单独上线让 recall 从 86.7 掉到 85.0，bigram 查询扩展同样是净退步）。两次都只有评测集能发现 |
| `626ddbd` | 新增 `ARCHITECTURE.md`（12 条工程决策 + 「哪些东西被测量过」+ 「明确还没做的部分」） |
| `4ab31c1` · `633d81a` | 还原被私有区字体挡住的薪资。⚠️ 第二个提交修的是「字形还原从来没生效过」的原因：`clean()` 里有一句删私有区字符，而接线写成了 `deglyph(clean(x))` —— 要还原的字符在还原之前就被删干净了 |
| `8a6884a` | 底色改米白，并按**实算对比度**修掉四处不达标（含 `--rule-strong` 1.58:1，而 WCAG 1.4.11 对 UI 组件边界要求 3:1 —— 这是我原本整个漏掉的一类） |
| `affd1a5` | 存 JD 抓鼠标停留的那条，并修掉 v1.3 一个把整页打瞎的 bug：`[class*='recommend']` 命中了 BOSS 列表页的**整页外壳**，于是每个字段都返回空 |
